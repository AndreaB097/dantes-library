/**
 * Package contenente alcune utility indispensabili per il funzionamento del
 * sistema: 
 * - DBConnection per la gestione delle connessioni con il database
 * mediante l'uso di JDBC.
 * - EmailUtility per l'invio di email mediante SMTP.
 * - BCrypt per la crifratura di stringhe.
 */
package danteslibrary.util;